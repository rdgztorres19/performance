# Node runner for the performance demos.
# No external dependencies: every demo uses only built-in modules.
FROM node:20-slim

WORKDIR /app

COPY . /app

RUN mkdir -p /data
ENV TMPDIR=/data

CMD ["node", "--version"]
